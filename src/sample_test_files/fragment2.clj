(ns sample-test-files.fragment2)

;; #####################################################
;; ###### Syntax error fragment - one per file  ########
;; #####################################################

(take (range) (range))
